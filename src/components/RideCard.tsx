type Props = {
  title: string;
  lines?: string[];
  compact?: boolean;
  onPress?: () => void;
};

export function RideCard({ title, lines = [], compact = false, onPress }: Props) {
  return (

  );
}