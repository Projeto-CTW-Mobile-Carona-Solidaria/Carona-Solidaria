import { Pressable, StyleSheet, Text, ViewStyle } from 'react-native';
type Props = { label: string; onPress?: () => void; style?: ViewStyle; cyan?: boolean };

export function PrimaryButton({ label, onPress, style, cyan = false }: Props) {
  return (

  );
}